"""AGP backend package.

Initialization is intentionally side-effect free.
Platform ownership and authorization are governed by the canonical server-side
AGP governance model. Legacy profile repair remains available only through its
explicit compatibility endpoint and must never run automatically on import.
"""
